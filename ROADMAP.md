# Pithy App — Roadmap: система прогресса и XP

## Концепция

Приложение построено вокруг фраз (например «I'm trying to please both»).
Каждая фраза — это **модуль** из нескольких уроков:

```
Глобальная карта (все фразы-модули)
  └── Модуль «I'm trying to please both»
        ├── Вводный урок       → +1 уровень, +1 unlock-очко
        ├── Тренажёр "trying"  → +XP, +1 уровень, +1 очко
        ├── Тренажёр "please"  → +XP, +1 уровень, +1 очко
        ├── Тренажёр "both"    → +XP, +1 уровень, +1 очко
        └── Босс-урок          → большой XP, модуль завершён → глобальная карта
```

**XP** накапливается внутри модуля (word_choice + phrase_assembly дают по 10 XP за верный ответ).
Когда XP достигает порога — прогресс-бар босса заполняется и замок снимается.
Все XP вкладываются в замок босса и обнуляются — остаются только **уровни** (глобальные).

---

## 🔄 Фаза 1 — Регистрация (В РАЗРАБОТКЕ)

Первый урок проходится без аккаунта. После завершения — появляется экран регистрации.
Прогресс из localStorage переносится в Supabase без повторного прохождения.

**Задачи:**
- [ ] Включить Email + Password (с подтверждением письма) в Supabase Auth
- [ ] Включить Google OAuth в Supabase Auth
- [ ] Компонент `RegistrationGate` — появляется после урока если пользователь не залогинен
  - Форма: имя + email + пароль
  - Кнопка «Войти через Google»
  - Ссылка «Уже есть аккаунт — войти»
- [ ] Экран входа (`LoginScreen`) для возвращающихся пользователей
- [ ] `useAuth` хук — `user`, `loading`, `signIn`, `signOut`, `signUp`
- [ ] Миграция гостевого прогресса из localStorage → Supabase после регистрации
- [ ] Защищённые экраны (карта, профиль) — редирект на вход если не залогинен

---

## Фаза 2 — XP визуально в уроке

**Задачи:**
- [ ] Счётчик XP в `PlayerTopBar` — `[⚡ 30 XP]` (только если урок имеет XP-ноды)
- [ ] Анимация `+10 XP` при верном ответе в `word_choice` и `phrase_assembly`
- [ ] Нода уже пройдена (localStorage) → анимации нет, XP не растёт (защита от фарма)
- [ ] Фраза-сборка 3-я попытка (ответ показан) → XP всё равно начисляется
- [ ] Экран итогов урока: суммарный XP + количество пройденных нод

---

## Фаза 3 — XP в Supabase

**Supabase таблицы:**
```sql
xp_events            -- лог: user_id, lesson_id, node_id, xp_earned
                     -- UNIQUE(user_id, lesson_id, node_id) — защита от повторов
user_module_progress -- user_id, module_id, xp_earned, intro_done, boss_done,
                     -- unlock_points, unlocked_ids[], completed_ids[]
user_global          -- user_id, level, completed_module_ids[]
```

**Задачи:**
- [ ] Создать таблицы в Supabase
- [ ] Батч-запись `xp_events` после завершения урока (`ON CONFLICT DO NOTHING`)
- [ ] Пересчёт `user_module_progress.xp_earned` и `user_global.level`
- [ ] Чтение прогресса при логине, синхронизация с localStorage
- [ ] XP за удалённые учителем ноды — сохраняется (не пересчитывается)

---

## Фаза 4 — Редактор модуля (для учителя)

**Supabase таблицы:**
```sql
phrase_modules         -- id, teacher_id, title, phrase_text,
                       -- intro_lesson_id, boss_lesson_id,
                       -- boss_xp_required (default 60),
                       -- xp_per_correct (default 10),
                       -- canvas_data JSONB (позиции нод)
phrase_module_lessons  -- id, module_id, lesson_id, pos_x, pos_y
```

**Задачи:**
- [ ] Новый экран `ModuleCanvas` — копия `CanvasPage` но ноды = уроки
- [ ] Три типа нод модуля: `intro` / `trainer` / `boss`
- [ ] Настройки модуля: `boss_xp_required`, `xp_per_correct`, текст фразы
- [ ] Визуальные связи между уроками (без триггерной логики)
- [ ] Сохранение модуля в Supabase

---

## Фаза 5 — Карта для пользователя

**Карта модуля (внутри фразы):**
- [ ] Read-only граф того же стиля что и редактор
- [ ] Статусы нод: заблокирован / доступен / пройден
- [ ] Разблокировка тренажёра = тратит 1 unlock-очко (пользователь выбирает порядок)
- [ ] Босс-нода: прогресс-бар XP с замком → при заполнении замок снимается

**Глобальная карта (все модули):**
- [ ] Сетка / граф всех фраз-модулей
- [ ] Статус модуля: не начат / в процессе / завершён (после босса)
- [ ] Переход внутрь модуля по тапу

---

## Ключевые правила XP

| Правило | Детали |
|---|---|
| Источники XP | `word_choice` + `phrase_assembly` (верный ответ) |
| Значение по умолчанию | 10 XP за ответ (настраивается в модуле) |
| Защита от фарма | UNIQUE(user_id, lesson_id, node_id) — только первое прохождение |
| 3-я попытка в сборке фразы | XP начисляется (ответ подсказан, но засчитывается) |
| Повтор урока | Разрешён (для практики), нода показывается как «пройдено», XP не начисляется повторно |
| XP при удалении ноды учителем | Сохраняется у пользователя |
| Уровни | Глобальные, +1 за каждый завершённый урок |
| XP vs уровни | XP тратится на разблокировку босса и уходит; уровни остаются навсегда |
| Офлайн | Уроки только онлайн |

---

## Диагностика вводного урока → цветные ветки

Вводный урок собирает данные об ошибках и скипах пользователя.
На основе этого карта модуля окрашивает ветки — фраза буквально **становится графом**.

### Что собираем

| Событие | Поле |
|---|---|
| Пролистал аудио/текст быстро (< 3 сек) | `skipped: true` |
| Ошибся в word_choice | `error: true, node_id` |
| Ошибся 2+ раз в phrase_assembly | `error_count: 2` |
| Верно с первой, не пропускал | `strong: true` |

Хранится в таблице `lesson_events` рядом с `xp_events`.

### Три ветки — три цвета

```
[ВВОДНЫЙ]
    │
    ├──🟠── I'm · trying   ← пропустил объяснение + ошибка
    │                         авто-открывается первым (первое очко тратится автоматически)
    │
    ├──🔵── to · please    ← были ошибки, но объяснение смотрел
    │                         рекомендуется вторым (значок «стоит пройти»)
    │
    ├──🟢── both           ← верно с первой, не пропускал
    │                         можно пропустить или взять бонусный XP
    │
    └──🔒── [БОСС]         ← заперт, прогресс-бар XP снизу
```

### Правила цветов

| Цвет | Условие | Поведение |
|---|---|---|
| 🟠 Оранжевый | ошибка + скип объяснения | авто-разблокируется первым unlock-очком |
| 🔵 Синий | ошибка ИЛИ скип (не оба) | значок «рекомендуем» на ноде |
| 🟢 Зелёный | верно с первой, не скипал | бонус +5 XP если пройдёт |
| ⚪ Серый | нет диагностических данных | нейтрально |

### Группировка слов в ветки

Несколько слов могут составлять одну ноду-ветку (например «I'm · trying» — оба оранжевые → одна нода).
Учитель задаёт группировку в редакторе модуля при расстановке тренажёров на canvas.

### Задачи (Фаза 5 расширение)

- [ ] Таблица `lesson_events` — запись диагностики вводного урока
- [ ] Алгоритм присвоения цвета ноде по `lesson_events`
- [ ] Карта модуля: цвет линии-связи и фон ноды по диагностике
- [ ] Авто-трата первого unlock-очка на оранжевую ноду
- [ ] Бонусный XP (+5) за прохождение зелёной ноды
- [ ] Значок «рекомендуем» на синих нодах
