-- Процент прохождения прямо в чекпойнте — чтобы «Мои уроки» (список/лента)
-- могли показать «на паузе, ~40%» без загрузки всего графа урока ради одной
-- цифры. Считается один раз на клиенте в момент чекпойнта (там граф уже в
-- памяти — LessonPlayer.jsx), здесь только хранится.
alter table public.lesson_progress
  add column if not exists pct integer not null default 0;
