-- Новое правило: если фото само по себе не может однозначно передать
-- время действия (прошедшее/настоящее/будущее/повторяющееся — застывший
-- кадр этого не показывает), подпись должна быть ЯВНЫМ ВОПРОСОМ,
-- указывающим на время, а не констатацией.
--
-- Проверено на конфликт: не пересекается ни с одним из существующих
-- правил (imagePrompt/note — про другое поле; про артикли и про audio→text
-- — про другие типы нод).
insert into public.lesson_rules (rule_text, sort_order, category)
select
  'Если фото само по себе не может однозначно передать время действия (прошедшее/настоящее/будущее/повторяющееся — застывший кадр этого не показывает), подпись должна быть ЯВНЫМ ВОПРОСОМ, указывающим на время, а не констатацией и не пересказом сцены. ✗ «Уже было» (утверждение, да ещё и почти дословно повторяющее сам ответ) ✓ «Когда это было?» / «Как часто?» / «Когда это будет?» — вопрос ставит время в фокус, и ученик сам называет его в ответе, а не считывает с картинки то, чего там нет. Фото при этом всё равно отвечает за КТО/ЧТО (сцену, героя, предмет) — вопрос лишь достаёт из ученика ту часть, которую фото передать не может.',
  47,
  'principle'
where not exists (
  select 1 from public.lesson_rules
  where rule_text like 'Если фото само по себе не может однозначно передать время действия%'
);
