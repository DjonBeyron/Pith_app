// Описание колоды карточек повтора для легенды обменного JSON (buildLegend).
// Отдельным файлом: lessonSchema.js упирается в потолок 400 строк
export const REVIEW_CARDS_DOC = {
  about: 'Колода карточек повтора урока-слова (система повторения «Моё обучение»). Необязательное поле верхнего уровня reviewCards: [{ nodes: [...] }]. Карточка — короткий «пересланный кусочек чата» из 1–3 нод той же схемы, что урок; в повторении ученик видит её отдельно от урока, вперемешку с карточками других слов',
  nodes: 'Ноды карточки — тот же формат, что nodes урока (ref, type, data, triggers). ref-ы ЛОКАЛЬНЫ для карточки: n1, n2 … в каждой карточке заново, переходы ведут только внутри своей карточки',
  rules: 'В каждой карточке ровно одно задание (word_choice, phrase_assembly, fill_blanks, photo_choice, table) и при необходимости 1–2 ноды контекста перед ним. Минимум 3 карточки на урок — иначе ответ заучивается. Карточки проверяют то же слово, что урок, но не повторяют задания урока дословно: другой контекст, другой порядок вариантов. Ноды-сигналы (signals) задания лежат в той же карточке',
}
