// Флаг «легенду приоритетов уже видели» (localStorage). Читает и ставит схема
// модуля (CurriculumView.jsx), снимает полный сброс модуля
// (useModuleAdminActions.js) — чтобы легенда показалась снова
export const LEGEND_SEEN_KEY = 'pithy_priority_legend_seen_v1'
