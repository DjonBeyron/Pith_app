// Нода «Таблица» — диктор-режим: ничего не рендерит в ленте чата.
// Весь UI — в TableDictatorPanel (LessonPlayer.jsx), как у word_choice/phrase_assembly.
export default function TableDictatorModule() { return null }
